name: photo_sharing_application
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1
environment:
  sdk: '>=2.18.2 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_core:
  firebase_auth: ^4.1.1
  provider: ^6.0.4
  cloud_firestore: ^4.0.4
  image_picker: ^0.8.6
  flutter_svg: ^1.1.6
  flutter_staggered_grid_view: ^0.4.1
  firebase_storage: ^11.0.4
  uuid: ^3.0.5
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/